# Blue slot upstream — app-blue container on the Docker network.
# Managed by blue-green-deploy.sh: copied to conf.d/upstream.conf when blue is active.
upstream backend {
  least_conn;
  server app-blue:3000 max_fails=3 fail_timeout=30s;
  keepalive 32;
}
