# Green slot upstream — app-green container on the Docker network.
# Managed by blue-green-deploy.sh: copied to conf.d/upstream.conf when green is active.
upstream backend {
  least_conn;
  server app-green:3000 max_fails=3 fail_timeout=30s;
  keepalive 32;
}
