# Active upstream — defaults to blue on first deploy.
# This file is overwritten by blue-green-deploy.sh at traffic switch time,
# followed by `nginx -s reload` to pick up the change in < 1ms.
upstream backend {
  least_conn;
  server app-blue:3000 max_fails=3 fail_timeout=30s;
  keepalive 32;
}
