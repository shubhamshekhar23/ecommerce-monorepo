-- Auth-service baseline: all tables and enums already exist from the backend migrations
-- (User, RefreshToken, OAuthAccount, PasswordResetToken, AuditLog, UserRole enum, OAuthProvider enum).
-- This migration is intentionally empty — it just registers the baseline state with Prisma.
SELECT 1;
