// Phase 5: replace with <CheckoutSkeleton /> once skeleton components exist.
export default function CheckoutLoading() {
  return (
    <div role="status" aria-label="Loading checkout" style={{ padding: '2rem', textAlign: 'center' }}>
      Loading checkout…
    </div>
  );
}
